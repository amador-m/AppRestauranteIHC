<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="8dp"
    android:layout_marginVertical="4dp"
    app:cardCornerRadius="8dp"
    app:cardElevation="2dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <ImageView
            android:id="@+id/ivAdminDishImage"
            android:layout_width="60dp"
            android:layout_height="60dp"
            android:scaleType="centerCrop"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            tools:src="@tools:sample/avatars"
            android:contentDescription="Imagem do prato" />

        <TextView
            android:id="@+id/tvAdminDishName"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:textStyle="bold"
            android:textSize="16sp"
            android:maxLines="1"
            android:ellipsize="end"
            android:layout_marginStart="16dp"
            app:layout_constraintStart_toEndOf="@id/ivAdminDishImage"
            app:layout_constraintEnd_toStartOf="@id/btnEditDish"
            app:layout_constraintTop_toTopOf="@id/ivAdminDishImage"
            tools:text="Nome do Prato Admin" />

        <TextView
            android:id="@+id/tvAdminDishPrice"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="14sp"
            android:layout_marginStart="16dp"
            android:layout_marginTop="4dp"
            app:layout_constraintStart_toEndOf="@id/ivAdminDishImage"
            app:layout_constraintTop_toBottomOf="@id/tvAdminDishName"
            tools:text="R$ 50,00" />

        <Switch
            android:id="@+id/switchDishAvailability"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Disponível"
            android:layout_marginTop="4dp"
            app:layout_constraintStart_toStartOf="@id/tvAdminDishPrice"
            app:layout_constraintTop_toBottomOf="@id/tvAdminDishPrice" />

        <ImageButton
            android:id="@+id/btnEditDish"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:src="@android:drawable/ic_menu_edit"
            app:tint="@color/brand_orange_secondary"
            android:background="?attr/selectableItemBackgroundBorderless"
            app:layout_constraintEnd_toStartOf="@id/btnDeleteDish"
            app:layout_constraintTop_toTopOf="@id/tvAdminDishName"
            app:layout_constraintBottom_toBottomOf="@id/tvAdminDishName"
            android:contentDescription="Editar prato" />

        <ImageButton
            android:id="@+id/btnDeleteDish"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:src="@android:drawable/ic_menu_delete"
            app:tint="@color/brand_orange_secondary"
            android:background="?attr/selectableItemBackgroundBorderless"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="@id/btnEditDish"
            app:layout_constraintBottom_toBottomOf="@id/btnEditDish"
            android:contentDescription="Excluir prato" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</com.google.android.material.card.MaterialCardView>